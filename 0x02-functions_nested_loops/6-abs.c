#include "main.h"
#include <stdio.h>
/**
 * _abs - function that compute absolute
 *@n: charcter absoluted
 *0n: takes an integer from other function
 *
 * Return: 0 (success)
*/

int _abs(int n)
{
if (n < 0)
n = (-1) * n;
return (n);
}
